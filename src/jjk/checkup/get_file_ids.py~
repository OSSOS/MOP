#!/usr/bin/env python

import MOPdbaccess,sys

cfhlsvw = MOPdbaccess.connect('bucket','lsadmin','MYSQL')
bucket=cfhlsvw.cursor()


sql="SELECT ra_rad,dec_rad, mjdate FROM cfeps.measure ORDER BY provisional,mjdate";

measures = bucket.execute(sql)

for measure in measures:


